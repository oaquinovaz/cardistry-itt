// Generated by CoffeeScript 2.0.2

(function() {
  //Si lo ponia mas abajo no me funcionaba, al parecer la posicion de la instruccion importa
  var input, scrollLink;

  scrollLink = $('.scr');

  input = $('input');  
 
  
//Para el focus y blur del icono de buscar
  $(" #buscarmov").on("focus", function(e) {
    $('#buscar-icono').addClass('box-shadow-blanco');
        
  });

  $(" #buscarmov").on("blur", function(e) {
    setTimeout(function(){
      $('#buscar-icono').removeClass('box-shadow-blanco');
  }, 85); 
  });

  //Para el modal de contribuir
  $(".dropdown-menu a").on("click", function(e) {
    
    $(this).parents(".menuparent").find('.selection').text($(this).text());
    $(this).parents(".menuparent").find('.selection').val($(this).text());

    if(e.target.id == 'cbf1')
    {
      $("#f1").css("display", "");
      $("#f3").css("display", "none");
      $("#f4").css("display", "none");
      $("#f2").css("display", "none");
      //$("#f2").toggle(); //Alternativa
    }

    if(e.target.id == 'cbf2')
    {
      $("#f1").css("display", "none");
      $("#f3").css("display", "none");
      $("#f4").css("display", "none");
      $("#f2").css("display", "");
      //$("#f2").toggle(); //Alternativa
    }

    if(e.target.id == 'cbf3')
    {
      $("#f1").css("display", "none");
      $("#f3").css("display", "");
      $("#f4").css("display", "none");
      $("#f2").css("display", "none");
      //$("#f2").toggle(); //Alternativa
    }

    if(e.target.id == 'cbf4')
    {
      $("#f1").css("display", "none");
      $("#f3").css("display", "none");
      $("#f4").css("display", "");
      $("#f2").css("display", "none");
      //$("#f2").toggle(); //Alternativa
    }
    
  });


  //Para la validacion del modal
  // $(".num").keypress(function (e) {
  //   //if the letter is not digit then display error and don't type anything
  //   if (e.which != 8 && e.which != 0 && (e.which < 48 || e.which > 57)) {
  //      //display error message
  //     $("#errmsg").html("Digits Only").show().fadeOut("slow");
  //     alert('efe');
       
  //   }
  //   })  
  
  $(".num").keydown(function (e) {
    // Allow: backspace, delete, tab, escape, enter and .
    if ($.inArray(e.keyCode, [46, 8, 9, 27, 13, 110, 190]) !== -1 ||
          // Allow: Ctrl+A, Command+A
        (e.keyCode === 65 && (e.ctrlKey === true || e.metaKey === true)) || 
          // Allow: home, end, left, right, down, up
        (e.keyCode >= 35 && e.keyCode <= 40)) {
              // let it happen, don't do anything
              
              return;
    }
    // Ensure that it is a number and stop the keypress
    if ((e.shiftKey || (e.keyCode < 48 || e.keyCode > 57)) && (e.keyCode < 96 || e.keyCode > 105)) {
      $('.num').tooltip({'trigger':'manual'});
        e.preventDefault();
        
    }
  });
  
  
  //$('#info').css 'border', 'solid 2px red'  
  /*
  $('#info').addClass('animated shake').one 'webkitAnimationEnd 
  mozAnimationEnd MSAnimationEnd oanimationend 
  animationend', ->
    $(this).removeClass 'animated shake'
    return
  return
  */
  /*
  scrollLink.click () ->  
  alert 'acabas de clickear'
  return

  input.blur () ->  
    if $(this).val() == ""
      $(this).css 'border', 'solid 2px red'
      $('#text').text 'Te falto introducir' 
    else
      input.addClass('input-nav')
      $('#text').text 'Gracias' 
    return

  scrollLink.hover (->
    $(this).css 'border', 'solid 2px red'
    return
  ), ->
    $(this).css 'border', 'solid 2px blue'
    return

  jQuery Chaining
    $('.box').fadeIn()
    $('.box').delay(2000)
    $('.box').css('background','red')
    $('.box').slideDown()

    $('.box').fadeIn().delay(1500).slideDown(2000).fadeOut()

    Codigo en CSS para que no aparezca al principio y se vea el fadeIn
    .box{
        display: none;
    }

  lastID = ""

  #$('.scr').fadeIn().delay(1500).slideDown(2000).fadeOut()

  $('.sb-toggle').click () ->
    lastID = $(this).attr("id")

  $('.sb-toggle').click (e) ->
    e.preventDefault() 

    if $(this).attr("id") == lastID
      $('#wrapper').toggleClass 'toggled'
    else

    return
   */  

  //$("#sidebar-wrapper").stick_in_parent($('#cat'))
  //$(".stik").sticky()
  $('#contribuirModal').click(e)(function() {
    e.preventDefault();
    $('#mod-content').addClass('animated zoomIn');
  });

}).call(this);